{
  "name": "shop-worker",
  "version": "1.0.0",
  "description": "Product Shop background worker — co 60s aktualizuje statystyki w Redis",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "test": "node --check worker.js && echo '✓ Syntax OK'"
  },
  "dependencies": {
    "pg": "^8.11.5",
    "redis": "^4.6.13"
  },
  "engines": {
    "node": ">=18"
  }
}
